#include "monty.h"
/**
 * s_stack - prints the top element
 * @head: pointer to the stack head
 * @line_num: line_number
 * Return: no return
*/
void s_stack(stack_t **head, unsigned int line_num)
{
	(void)head;
	(void)line_num;
	bus.lifi = 0;
}
